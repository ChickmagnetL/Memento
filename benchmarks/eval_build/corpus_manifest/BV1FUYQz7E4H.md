# PostgreSQL能存万物！这还是你认识的数据库吗？
- video_id: BV1FUYQz7E4H
- document_id: 1661a6ae3c5949dfb420de3690f673f5
- platform: bilibili
- duration: 1327s (22m07s)
- chunk_count: 12
- language_guess: zh

## L3 brief
本视频系统介绍了 PostgreSQL 的安装配置、高级数据类型、表继承，以及其在 JSON 存储、向量检索、全文搜索和 API 扩展方面的强大应用。

## L2 summary
本视频深入介绍了开源关系型数据库 PostgreSQL 的强大功能与其作为全能后端平台的潜力。视频涵盖了 Postgres 在 Ubuntu 上的快速安装配置、基于 schema 的权限控制，以及对网络网段、几何运算、自定义类型和表继承等面向对象特性的原生支持。此外，视频重点演示了如何使用 JSONB 替代 NoSQL，利用原生及 PGroonga 插件实现中英文全文检索，结合 pgvector 与 LangChain 构建大模型 RAG 向量检索系统，并借助 pg_cron 执行定时任务。最后，视频探讨了通过 UNLOGGED 临时表替代 Redis 缓存，以及利用 PostgREST、pg_graphql、Apache AGE 和 TimescaleDB 扩展将 PostgreSQL 转化为 REST/GraphQL API、图数据库及时序数据库的实践方案。

## Chunks
### chunk 0 [00:00]
PostgreSQL能存万物！这还是你认识的数据库吗？

- Platform: bilibili
- Video ID: BV1FUYQz7E4H
- Source URL: https://www.bilibili.com/video/BV1FUYQz7E4H/?
- Author: 技术爬爬虾

[00:00] 这是世界上最先进的开源关系型数据库。PostgreSQL能够装下世间万物，其能力早已超越了传统关系型数据库的范围。现代 Web 开发为了解决各种问题，各类花哨的工具层出不穷。其实，仅仅一个关系型数据库 PostgreSQL 就能覆盖大部分后端开发需求。本期视频我们从安装入门开始，介绍一系列 PostgreSQL 的强大功能，一起来看看为什么 Postgres 是当今最先进、功能最丰富的关系型数据库。
[00:32] 与 MySQL 的快速应用的卖点不同，Postgres 的开发理念是要打造出功能最丰富、最符合 SQL 标准的数据库。凭借着每年一个版本的稳定迭代、高标准的 SQL 兼容性、高可用性、高扩展性和自定义能力，在最近几年的 Stack Overflow 调查中，渐渐超越了 MySQL 登顶了最受开发者欢迎的数据库。总体来说，Postgres 比 MySQL 更符合 SQL 标准，尤其是在 SQL 语言特性和高级功能方面。比如 Postgres 支持把表结构的修改语句使用事务管理，Postgres 支持部分索引、支持可延迟约束等等。接下来我们先安装一下 Postgres，然后边用边聊。
### chunk 1 [01:14]
PostgreSQL能存万物！这还是你认识的数据库吗？

Postgres 支持把表结构的修改语句使用事务管理，Postgres 支持部分索引、支持可延迟约束等等。接下来我们先安装一下 Postgres，然后边用边聊。[01:14] 本期视频我们不涉及企业级高可用版本的 PostgreSQL，企业级数据库有专门的 DBA 负责。我们简单来看一下个人开发者如何快速上手安装。这是我的一台 Ubuntu 系统的云服务器，我们先执行第一个命令 `sudo apt update` 更新一下 APT 索引，然后 `sudo apt install postgresql` 安装一下数据库，后面还有一个 postgresql-contrib，这是一个官方提供的插件包，里面有很多好用的工具。安装完成以后，系统会自动创建一个 postgres 用户，然后使用 `sudo -i -u postgres` 切换到新创建的用户。接下来执行 `psql` 进入 Postgres 的命令行环境。`\password postgres` 给初始化账户设一个密码，这样基本配置就完成了。为了方便调试，我们要放开数据库的远程连接功能，让我的本地电脑可以连接到服务器的数据库。在 Postgres 的命令行下面执行这两个命令，显示一下两个配置文件的地址。我们把这两个配置文件的地址记录一下，执行 `EXIT` 退出 Postgres 命令行。使用 VI 命令，先修改第一个配置文件，找到 `listen_addresses`，把它改成星号 `*`，表示监听所有可用网络。接下来我们使用 VI 命令来修改第二个配置文件，这里找到 IPv4 这一行允许连接的 IP 地址，这里我全部设成 0，表示数据库允许所有 IP 地址的客户端进行连接。最后输入命令 `systemctl restart postgresql` 把数据库重启一下，这样安装加配置就完全成功了。
### chunk 2 [02:44]
PostgreSQL能存万物！这还是你认识的数据库吗？

允许所有 IP 地址的客户端进行连接。最后输入命令 `systemctl restart postgresql` 把数据库重启一下，这样安装加配置就完全成功了。[02:44] 接下来我们找一个数据库工具来连接一下。这里我使用的数据库工具是开源免费的 DBeaver。我们打开软件，在左边创建连接，找到 PostgreSQL，点击下一步。这里主机输入我们服务器的 IP 地址，端口还是使用默认端口 5432。这里注意一下，要在服务器的防火墙那边把 5432 端口放开。密码填写刚才我们配置的密码，这样点击完成，双击连接一下，DBeaver 会自动下载数据库驱动。我们看到 Postgres 比下面的 MySQL 多了一个层级，这里叫做 schema。Postgres 是数据库、schema 加表这么三级，MySQL 则是只有数据库跟表这么两级，这也是它们俩的一个显著区别。
### chunk 3 [03:29]
PostgreSQL能存万物！这还是你认识的数据库吗？

了一个层级，这里叫做 schema。Postgres 是数据库、schema 加表这么三级，MySQL 则是只有数据库跟表这么两级，这也是它们俩的一个显著区别。[03:29] 在 Postgres 的官网介绍里面说，这是一个 open source object-relational database，也就是开源的对象关系型数据库。这里面有两个关键词：对象与关系。Postgres 既有传统数据库（比如 MySQL）中表、行、列等严谨的关系形式，又吸收了面向对象的编程思想，直接在数据库层面提供对象的支持。当我们需要创建一个新的数据库列的时候，看到 Postgres 里面提供了上百种内置的数据类型，每种数据类型就是一个内置的数据库对象。仅仅一个时间相关的就有这么多，有带时区和不带时区的，还有带日期的、不带日期的，能想到的时间格式基本全部都内置了。我们来看一个例子，首先创建一张表。这里的 CIDR 字段是 Postgres 一个特有的数据类型，用来存储一个网段。接下来我们往表里插入数据，我们可以直接往表里存储一个网段。让我执行一下。数据库支持存储网段，查询的时候也非常的方便，两个大于号 `>>` 作为一个运算符代表网段里面的包含关系。这个 SQL 语句用来查找包含这个子网的所有网段。这样我们执行一下，查询到了结果。除了原生支持网段数据类型，Postgres 还内置了存储 IP 地址、MAC 地址等等跟网络相关的数据类型。Postgres 还有一套跟几何运算相关的数据类型，比如点、线、线段、矩形、路径、圆形等等，支持直接使用 SQL 语句进行简单的几何运算跟查询。Postgres 还支持把数组或者多维数组作为数据类型。除了 Postgres 内置的数据类型可以用来存储对象，它还支持直接使用 SQL 语句来自定义类型。比如这里我定义一个叫做 `employee` 的类型，里面有名字、年龄还有技能这三个属性，这样执行一下。接下来我们创建一个新的表叫 `employees`，它有一个 ID，第二列就是我们刚才自定义的类型，这样执行一下。接下来我们往新创建的表里面增加记录。我们先定义这么一行，然后使用类型转换，把它转换成 `employee` 的类型，这个类型就可以直接存到 `employees` 表的对应字段里面。我们看一下最终的效果，打开 `employees` 表，它有两列，一列是 ID，还有一列是我们的自定义类型。自定义类型里面又包含了 name、年龄还有技能，技能又是一个数组，我们还可以把这个数组展开。在传统的关系型数据库里面，数据存储在扁平的二维化表格里面；而面向对象编程的世界观里面，对象之间通过组合、继承形成复杂的网状的层次化关系。把复杂的对象压扁到二维表格里面，是一个繁琐、易出错且效率低下的工作，这个难题在业界被称为阻抗不匹配。Postgres 有自己独特的设计哲学，它在数据库层面引入对象的概念，使得数据库能够更好地理解和存储面向对象编程中复杂的数据结构和行为。Postgres 支持表之间定义继承关系，我们可以定义一个 `developer` 的表代表公司里面的软件开发人员，它有一个自己独特的字段，就是掌握的编程语言。这个表继承自父表 `employee`，所以它又有父表里面的全部字段。然后我们往里面插入一条数据，我们打开 `developer` 表，这里面有继承自父表的字段，还有它自己的独特字段。而且我们新加的这条数据，它也会同步地出现在父表里面。比如我们为公司招聘一个开发人员，那他首先肯定是公司的员工。这个特性就模拟了面向对象编程里面的多态性，更好地反映了类之间的继承关系。
### chunk 4 [07:06]
PostgreSQL能存万物！这还是你认识的数据库吗？

，它也会同步地出现在父表里面。比如我们为公司招聘一个开发人员，那他首先肯定是公司的员工。这个特性就模拟了面向对象编程里面的多态性，更好地反映了类之间的继承关系。[07:06] Web 开发者们经常讨论 SQL 数据库与 NoSQL 数据库。NoSQL 数据库的一个最大卖点就是可以定义非结构化的数据，比如每条记录的 JSON 格式都有可能不同。在 SQL 数据库中就不能这么做，因为每张表都有固定的列格式。然而，PostgreSQL 原生支持 JSON 数据类型，很大程度上能够替换 MongoDB 这类 JSON 文档数据库。我们来看一个例子，我们创建一个表用来存储 HTTP 请求的日志信息，有一个 ID 字段，还有一个 `data` 字段，这里的数据类型是 JSONB，也就是二进制类型的 JSON，这是 Postgres 里面存储 JSON 数据最好的类型。有了表以后，我们就可以往里面插入数据。这个 JSON 定义可以非常灵活，随意定义，里面的属性都可以自由增减，包括可以进行嵌套，还可以定义数组等等，我把它插入进去。接下来我们可以对这个 JSON 数据进行查询，这个双箭头 `->>` 是一个操作符，表示提取。我提取 JSON 数据里面 IP 地址等于它的，然后放到 WHERE 语句后面，把所有匹配的数据全部查询出来。如果我们经常需要这么查询，可以建一个表达式索引，先把 remote address 提取出来，然后在上面使用 B-Tree 建一个索引，这样可以大幅度提高这个表达式的查询效率。JSONB 上面还有一种非常高效的建立索引的方式，就是直接对整个 JSON 字段建立一个 GIN 索引。GIN 索引可以大幅加快对于 JSON 内部数据的查询速度。比如我们可以使用这个索引查找 status 等于 200 的数据，使用这个索引查找 key 里面包含 request data 的数据。视频里讲的是 Postgres 有这个能力可以这么做，但是具体到项目里是不是应该拿它替换掉 MongoDB，这个还是需要根据项目的具体情况而定，对于后面那些可以替换的功能同理。
### chunk 5 [08:55]
PostgreSQL能存万物！这还是你认识的数据库吗？

ostgres 有这个能力可以这么做，但是具体到项目里是不是应该拿它替换掉 MongoDB，这个还是需要根据项目的具体情况而定，对于后面那些可以替换的功能同理。[08:55] 有一种常见的业务需求就是对文本进行全文检索。比如我要检索哪一句话有 powerful 这个单词，SQL 语句里面的 LIKE 语法并不能满足需求。对于这种任意位置匹配的全文检索，数据库无法使用索引，只能进行全表扫描，性能极低。这时候通常能够想到的方案就是 ElasticSearch。不过 ElasticSearch 部署繁琐，而且增加了系统复杂度。对于全文检索，Postgres 也有不错的支持方案。我们先看第一个原生的方案实现全文检索的需求。我们先创建一个文档表，接下来给这个文档表增加一些测试数据。这步是重点：我们新加了一个列 tsvector，类型是 tsvector 文本搜索向量，这是 Postgres 里面一个独有的数据类型；接下来定义了这个列是 `GENERATED ALWAYS`，表示总会把这个 `content` 字段转成向量的形式保存在数据库里面；这里的 `to_tsvector` 是一个内置方法，可以把文本内容转换成文本搜索向量，这里使用的语言是英文。这个转换过程非常智能，它会自动进行分词，转换成小写，移除掉定冠词、介词等等对搜索意义不大的单词，还能进行词干提取，去掉时态、单复数等等，把它还原成词根的形式。我们进入数据库看一下现在的表长什么样：现在有三列，第一列 ID，第二列是内容，第三列就是向量化后的数据，使用这个数据可以更方便地进行全文检索。下一步我们在向量列上面建索引，这里建 GIN 索引。最后一步就是进行搜索，我们先把要搜索的字符也转换成向量的形式，两个 `@` 符号 `@@` 表示包含。这句话的意思是说，要搜索内容包含 powerful 的短句。我们来执行一下，成功搜索到了内容，而且这个搜索效率是非常高的。Postgres 的原生分词器只支持英文、法语、德语、西班牙语等等，并不支持中文。我们可以下载一个中文分词器的插件，让它的向量化支持中文。Postgres 有非常完善丰富的插件生态，这个 awesome-postgres 里面列举了很多相关的插件，我们可以找到这里的 Extensions，这里面有各式各样的插件，为 Postgres 提供了各式各样的功能，有着非常活跃的社区生态。接下来我们不再使用向量的方式进行全文检索，我们下载一个插件来完成全文检索的需求。这里我下载的插件是 Pgroonga，这里点击 Install，选择自己的操作系统，我是 Ubuntu 24.04，只需要把这些命令执行一下，我们就可以把插件安装上。来到云服务器输入第一个命令、第二个命令、第三个命令、第四个命令，Ubuntu 24.04 输入最下面的命令。
### chunk 6 [11:31]
PostgreSQL能存万物！这还是你认识的数据库吗？

，只需要把这些命令执行一下，我们就可以把插件安装上。来到云服务器输入第一个命令、第二个命令、第三个命令、第四个命令，Ubuntu 24.04 输入最下面的命令。[11:31] 插件安装完成以后，我们先执行这个 SQL 语句把插件启动起来，然后往我们的测试表里面再加三行中文的测试数据。接下来我们不需要创建向量列了，可以直接创建索引。这里我们使用的索引类型就是 PGroonga，然后在文本字段上面直接创建索引。接下来就可以进行检索，这是 PGroonga 自定义的一个操作符表示包含，我们搜索包含“引擎”这两个字的文本。这里成功搜索到结果，这样我们就通过安装插件的形式添加了中文的全文搜索能力，还是非常简单的。
### chunk 7 [12:06]
PostgreSQL能存万物！这还是你认识的数据库吗？

ga 自定义的一个操作符表示包含，我们搜索包含“引擎”这两个字的文本。这里成功搜索到结果，这样我们就通过安装插件的形式添加了中文的全文搜索能力，还是非常简单的。[12:06] 目前大模型知识库最常用的方法是 RAG，也就是检索增强生成。当用户把资料添加进知识库的时候，程序会先把它们拆分成很多个文本块，然后使用嵌入模型对这些文本块进行向量化。向量化指的是把切分后的文本变成一个超长的数字序列。然后程序把向量以及对应的文本保存在向量数据库里面。接下来用户开始提问，不过这个提问并非直接送达到大模型那里，而是把问题本身也经过向量化处理，然后把用户提问与向量数据库进行相似度匹配。这个匹配过程是基于向量的纯数学运算。最后，知识库选出匹配度最高的几个原文片段，再加上用户的问题发给大模型，大模型进行最后的归纳总结。在整个 RAG 系统里面，向量数据库的选择至关重要。我们当然可以选择 Postgres 作为非常可靠的向量数据库。想把 Postgres 作为向量数据库，需要先安装一个插件 `pgvector`。我们来到服务器执行这一行命令就可以安装完成插件。接下来我们往里存点数据试一下，还是老规矩，我们先执行这个命令把插件启用起来。然后我们新建一个表，有两个字段 ID 和 `embedding`，第二个字段的类型是一个维度为 3 的向量。接下来我们往表里插数据，向量就这么写就可以了。我们来到表里面看一下，看到 ID 还有向量。这样我们的 Postgres 就已经准备好作为一个向量数据库进行服务了。接下来我们使用 Python 语言来实现一个 RAG 系统，体验一下如何使用 Postgres 的向量数据库。在 AI 开发框架 LangChain 里面有一个叫做 pgvector 的章节，里面介绍了如何使用 Postgres 的向量数据库搭配嵌入模型打造一个 RAG 系统。我们看到里面给了一些代码样例：首先安装 langchain-postgres 依赖，下面安装 langchain-openai 的依赖，这个主要是因为要用 API 调用嵌入模型。我把它的这些代码整理了一下，然后写了一个样例文件。首先是两个环境变量，这里我用的 AI 平台是硅基流动，我选择的模型是 BGE-M3，有关如何在硅基流动上面选择嵌入式模型，在这期视频里面有过详细介绍了。我们只需要把这三个配好：首先是硅基流动的 API key，硅基流动的 API 端点，接下来是模型的名称。connection 这里配置 Postgres 的数据库连接地址，这里是用户名，后面是密码，后面是服务器的 IP 地址；然后 collection name 随便起一个。接下来创建一个向量存储库，然后下面是一些测试数据，这里面有很多句话，比如“猫在池塘里”、“鸭子在池塘里”、“新鲜的苹果在市场里”等等这些测试数据。接下来两句话，首先把测试数据循环一下添加到数据库里面，然后就可以使用数据库的查询。使用这个查询 `similarity_search_with_score`，就是查询上面的测试数据里面跟这句话匹配度最高的，`k=10` 是总共找 10 句话，最后把结果跟分数都打印出来。我们来试一下，把代码运行一下，看到它成功完成了召回，找到了跟“可爱动物在水里”最相近的这句话，就是“猫在池塘里面”。前面是分数，分数越低表示两个向量之间的距离越近，也就是相似度最高，这样成功通过向量数据库完成了文本召回。接下来就很简单了，我们把相似度最高的前面几条数据发给 AI，让 AI 帮我们做一个总结，一个 RAG 系统就搭建完成了，还是非常方便的。最后我们来看一下数据库里面是怎么存的。我们看到 LangChain 为我们自动创建了两个表：collection 里面定义了我们的数据集，这里我随便起了个名字；接下来就是 embeddings，embeddings 里面存储了原始的句子还有它对应的向量。我们看到它的向量是这个样的，是一串非常长的数字。这里我使用的嵌入模型是 1024 维的，所以这里对应的向量也是 1024 维的。有关 RAG 系统的一些详细概念，建议大家再参考一下这期视频，这里我就不赘述了。
### chunk 8 [16:05]
PostgreSQL能存万物！这还是你认识的数据库吗？

这里我使用的嵌入模型是 1024 维的，所以这里对应的向量也是 1024 维的。有关 RAG 系统的一些详细概念，建议大家再参考一下这期视频，这里我就不赘述了。[16:05] 在实际业务开发中，有时经常需要编写一些比如定时数据清理等等这样的 cron job。我们可以使用 Java、Python 等编程语言编写一个定时任务，上传到公司代码库，打包成 Docker 镜像，最后再弄个 K8s 把它跑起来，还得再分配专人去维护。当然我们也可以直接在 Postgres 里面一行 SQL 搞定定时任务。我们先来到服务器把插件安装一下，这里的数字 16 是我当前的 Postgres 数据库版本。接下来我们去服务器上修改一下 Postgres 的配置文件，找到最下面添加两句话。我们根据 GitHub 首页的教程，先把这一句话复制过来添加上，然后下面这句话复制过来添加上，保存一下。还需要修改这个 HBA 的配置文件，在 IPv4 上面添加这么一行，也就是使用本地环路地址连接数据库的时候不再需要密码验证，这样再保存一下，重启一下数据库。然后跟之前一样，我们使用一个 SQL 语句把插件功能开启起来，然后我们就可以来定义定时任务。这里使用这个 SQL 语句 `select cron.schedule`，第一个参数我们给任务定一个名字；第二个参数就是 cron 表达式，它是怎么定时执行的，在这个例子里面就是每天凌晨 3 点；在这四个美元符号 `$$` 的中间可以插入想定时执行的命令。接下来我想把 document 表里面的所有内容全部插入 document_archive 表里面，然后把 document 表里面内容删除，也就是每天晚上 3 点我把 document 表的全量数据备份到 document_archive 里面，再把原来的 document 表清理掉，这样我们的定时任务就设置好了。我们也可以直接使用这个语句来修改之前定义好的定时任务，保持它的 name 不变，我们可以直接来修改定时还有里面的具体执行方法。比如这里我把定时全部改成星号 `*`，就让它每分钟执行一次。好，接下来可以等待一分钟看看有什么效果。可以看到这个 document 表里面的所有数据都被清空了，然后被归档到 document_archive 表里面了。在 cron 这个 schema 里面有两个表：job 表里面列出了我们自定义的 scheduled job，然后下面的 job_run_details 表里面就是它的执行记录，包括执行的命令、后面的执行结果还有返回值等等。这样我们一条 SQL 语句就完成了数据库定时清理的工作，非常方便。
### chunk 9 [18:21]
PostgreSQL能存万物！这还是你认识的数据库吗？

run_details 表里面就是它的执行记录，包括执行的命令、后面的执行结果还有返回值等等。这样我们一条 SQL 语句就完成了数据库定时清理的工作，非常方便。[18:21] Redis 是一个非常高效的内存数据库，我们一般把它当作缓存使用。我们也可以不使用 Redis，使用 Postgres 作为轻量级的高速缓存。我们可以在建表的时候加入一个 UNLOGGED 的关键字，这个关键字让新建出来的表不写入 WAL 日志，这样可以提高 5 倍左右的写入速度，但是代价是一旦数据库断电或者意外崩溃，表里面的数据全部丢失。这种 UNLOGGED 的表非常适合作为临时表或者缓存使用，可以大幅提高写入速度。然后可以在 Postgres 的配置里面把这个共享缓冲区调大，这样经常读写的数据就会保存在内存的缓存里面，大幅度地提高性能。然后我们还可以搭配刚才讲到的定时任务技巧，建立一个定时清理缓存的定时任务，把所有的过期缓存全部删除掉。以上这些方法配合起来，就能获得一个性能很不错的内存缓存表，在一定程度上就可以代替 Redis 的一些功能。
### chunk 10 [19:17]
PostgreSQL能存万物！这还是你认识的数据库吗？

个定时清理缓存的定时任务，把所有的过期缓存全部删除掉。以上这些方法配合起来，就能获得一个性能很不错的内存缓存表，在一定程度上就可以代替 Redis 的一些功能。[19:17] PostgREST 是一个开源软件，它可以把 Postgres 数据库直接转换成 RESTful API，数据库的表结构决定了 API 的端点跟操作。我们来到项目的 GitHub 首页，右边有一个 Release，这里我以 Windows 系统为例，我下载这个 Windows 版的可执行程序。下载完成以后，我把它解压到桌面。我们使用 SQL 语句创建一个下级角色，接下来赋予这个角色 public schema 的权限，然后赋予这个角色可以执行 SELECT 语句在 document 这个表上面的权限。Postgres 角色权限控制力度非常精细。接下来我们创建一个配置文件 `sample.conf`，在这里面主要写三行：第一行是数据库的连接地址，这是账号名，后面是密码；然后 db-schemas 下面就是我们刚才定义的角色，这样我们保存一下。然后我们在桌面开一个命令行，执行这个命令，前面是可执行文件，后面是配置文件，回车，这样它成功连接上了服务器的数据库。我们在 document 表里面造一些测试数据，然后在浏览器输入 localhost 端口 3000 后面斜杠直接加表名，这就是 PostgREST 生成的一个 API 接口。我们使用这个 API 接口直接就能访问到表里面的数据。我们可以给这个角色配置上 UPDATE 或者 INSERT 的权限，然后就可以使用对应的 PUT 跟 POST 接口往表里面增加或者修改数据，这样都不需要写业务代码了，直接把 Postgres 数据库转换成了 RESTful API，非常方便厉害。这个项目还有很多其他的进阶功能，比如过滤、排序、分组、权限验证等等。pg_graphql 可以让 Postgres 数据库获得 GraphQL API 的能力，我们只需要把插件安装一下，然后使用 SQL 语句把插件启动起来。接下来我们可以使用 `graphql.resolve` 里面输入 GraphQL 的查询语句来查询数据库，最终得到一个美观的 JSON 对象。
### chunk 11 [21:09]
PostgreSQL能存万物！这还是你认识的数据库吗？

句把插件启动起来。接下来我们可以使用 `graphql.resolve` 里面输入 GraphQL 的查询语句来查询数据库，最终得到一个美观的 JSON 对象。[21:09] Apache AGE 是一个 Postgres 扩展，它允许我们直接使用 Cypher 语言来建立图的数据。比如我们可以使用图数据处理好友之间的关系网，使用图数据处理电网之间的关系，有了这个扩展以后我们就不需要再安装专门的图数据库了。TimescaleDB 是一个 Postgres 的扩展，它可以给数据库赋予时序数据库的能力。我们可以使用这个安装步骤把这个扩展安装到数据库里面，这样就为 Postgres 增加了时序数据库的能力，适合处理一些物联网、金融数据等对时序要求非常高的数据。
[21:44] 本期视频我们介绍了一系列 PostgreSQL 的强大功能。PostgreSQL 的能力早已超越了传统关系型数据库的范围，而更像是一个全能的后端聚合平台。之后我还会再单开一期视频，聊聊 Postgres 在 SQL 高级特性上比起 MySQL 的功能优势。本期视频就到这里，感谢大家，我们下期再见。
