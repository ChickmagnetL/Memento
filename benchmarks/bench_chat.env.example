# Copy to bench_chat.env and fill in your endpoint/model/key.
# bench_chat.env is gitignored — never commit the real key.
export MODELS__CHAT__MODEL=mimo-v2.5-pro
export MODELS__CHAT__ENDPOINT=https://your-endpoint.example/v1
export MODELS__CHAT__API_KEY=sk-your-key-here
